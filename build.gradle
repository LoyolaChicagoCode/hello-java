apply plugin: 'java'
apply plugin: 'application'
mainClassName = "hw.MainClass"

repositories {
    google()
    mavenCentral()
}

dependencies {
    testImplementation 'junit:junit:4.+'
}

tasks.withType(JavaCompile) {
    options.compilerArgs << "-Xlint:unchecked"
}